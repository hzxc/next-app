import { ReactNode } from 'react';

export const ConfirmSwap: React.FC<{
  visible: boolean;
  close: () => void;
  children: ReactNode;
}> = ({ visible, close, children }) => {
  return <div></div>;
};
